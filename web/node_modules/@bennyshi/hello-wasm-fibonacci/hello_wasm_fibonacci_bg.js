import * as wasm from './hello_wasm_fibonacci_bg.wasm';

/**
* @param {number} n
* @returns {number}
*/
export function get_fibonacci(n) {
    var ret = wasm.get_fibonacci(n);
    return ret >>> 0;
}

